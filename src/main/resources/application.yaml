server:
  port: 8081
  servlet:
    context-path: /social-network

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/social_network"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
